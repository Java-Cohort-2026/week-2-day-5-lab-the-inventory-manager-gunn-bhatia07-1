# W2 Friday Lab: The Inventory Manager

### Your Mission
1. Open `src/Item.java` and declare the three required attributes so that `Main.java` can compile.
2. Open `src/Main.java`. Write the `calculateTotalValue` method to figure out the total financial value of all goods in the array. 
3. Write the `findItem` algorithm to locate a specific item's index by comparing strings.
